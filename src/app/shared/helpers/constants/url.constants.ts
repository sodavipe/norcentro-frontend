import { environment } from "src/environments/environment";


export const INVENTORY_URL = `${environment.API_BACKEND}motos`

export const TYPE_DOCUMENT_URL = `${environment.API_BACKEND}tipo-documentos`

export const USER_URL = `${environment.API_BACKEND}usuarios`

export const SUPPLIER_URL = `${environment.API_BACKEND}proveedores`